# Deploy Scripts

## ec2-deploy.sh

Main deployment script for EC2 instances.

### Usage

```bash
./scripts/deploy/ec2-deploy.sh
```

### Prerequisites

- AWS CLI configured with appropriate credentials
- SSH key pair for EC2 access
- Required environment variables set in `.env`
